import React from 'react'

import spinner from '../../spinning-loading.gif'

function Spinner() {
    return (
        <div>
            <img src={spinner} alt="" />
        </div>
    )
}

export default Spinner